#include <stdio.h>              // include information about standard library
                                
int main()                      // define a function named main that receives no argument values
{                               // statment of main are enclosed in braces
    printf("Hello, World\n");   // main calls library function printf to print this sequence of
    return 0;                   // characters; \n represent the newline character
}